"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.22.49"
VERSION_LABEL = "v2.22.49 — ХОТФИКС регрессии из v2.22.48: кнопка «🎲 Случайный ход» (scene_battle_ext1.js → _onAuto) снова делает СЛУЧАЙНЫЙ ход, а не пропуск. В шаге 3/3 я ошибочно слил две вещи: кнопку случайного хода и таймаут. Теперь разделено: _onAuto (кнопка) = случайный ход через _submitChoice; новый _onTimeout (таймер 15с в scene_battle_ext2b.js) = честный пропуск через /api/battle/timeout. Премиум-АВТОБОЙ (bot_battle_autobattle.js) не был затронут — он и так ходит через _submitChoice. Только webapp, серверная логика не менялась. version.py →2.22.49, GAME_VERSION →17.99."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "17.99"
