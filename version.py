"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.22.39"
VERSION_LABEL = "v2.22.39 — аудит экипировки, безопасные фиксы (дыры 2/3/4). (2) eq_stats в /api/player теперь отдаёт body_def_pct/reflect_pct/block_chance — раньше защита тела/шипы/блок брони работали в бою, но НЕ доходили до фронта (видны были только текстом карточки). (3) regen_speed_pct ускоряет реген и В БОЮ (execute.py: regen × (1+speed/100), start.py::_eq_regen_speed_pct) — в пару к восстановлению вне боя. (4) карточка «Экипировка» в профиле (stats_html_overlay_pages.js) теперь показывает ВСЕ бонусы: + защита тела/шипы/блок/анти-крит/двойной/анти-уворот/глушение/замедление/скорость регена/золото/опыт (было 12 полей → стало 23). Аудит подтвердил: 1на1/бот/AFK/Натиск применяют всю экипировку. ОСТАЁТСЯ дыра 1 — мировой босс игнорирует поштучные статы предметов (своя формула damage_calc.py), чинить отдельно по запросу. version.py →2.22.39, GAME_VERSION →17.89."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "17.89"
