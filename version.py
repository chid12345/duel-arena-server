"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.22.26"
VERSION_LABEL = "v2.22.26 — апгрейд/разборка: (1) разборка теперь ТРАТИТ предмет (удаляет из коллекции player_owned_weapons/armor2 + моды легендарной mythic4) — раньше шарды давались, а вещь оставалась; (2) системный confirm заменён на стилизованное окно в стиле игры; (3) на карточках брони показывается значок прокачки «+N» и усиленные статы (бой их уже учитывал, не хватало только отображения) через общий common/plus_badge.js. Корневой фикс ранее: AmbiguousColumn в ON CONFLICT (PostgreSQL) ронял разборку/апгрейд и недельные рейтинги ВБ/Натиска. version.py →2.22.26, GAME_VERSION →17.76."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "17.76"
