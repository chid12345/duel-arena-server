"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.6.86"
VERSION_LABEL = "v2.6.86 — Голова/Профиль: убрана связка clean+preFX, ронявшая PNG в «белый квадрат» на Android WebView. + золотой ободок-корона у Профиля (виден всегда). + долгая загрузка: lazy-PNG больше не блокирует UI (filecomplete вместо общего complete + 1.5с задержка фоновой пачки). + В БОЙ: fallback-пересборка battle-панели."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "11.56"
