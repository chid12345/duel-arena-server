"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.6.87"
VERSION_LABEL = "v2.6.87 — Профиль: tab_profile.png был RGB (без альфы) → лев показывался на белом фоне = «белый квадрат»; заменён прозрачным RGBA из foto/нижнее меню/профиль.png. + preFX убран со ВСЕХ слотов экипировки: Android WebView лотерейно ронял случайный слот (раз шлем, раз броня) в «белый квадрат». + сохранение пропорций PNG в слотах (object-fit: contain) — шлем больше не сплющивается в тёмный квадрат."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "11.57"
