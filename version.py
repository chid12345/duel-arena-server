"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.6.99"
VERSION_LABEL = "v2.6.99 — fix: «В БОЙ» — 3 бага. (1) Натиск запускался без проверки HP<15 → сервер возвращал ошибку уже внутри сцены. (2) Кнопка «Мои вызовы» подписана «Входящие вызовы» — исправлено на «Исходящие вызовы» (вызывает /challenge/outgoing). (3) _dailyClaimBusy не сбрасывался в shutdown → после scene.restart() ежедневный бонус нельзя было получить повторно."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "11.69"
