"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.0.48"
VERSION_LABEL = "v2.0.48 — perf: lazy-load PNG экипировки с приоритетом — надетые 6 вещей (~3МБ) грузятся первыми, остальные 50МБ фоном; скины на профиле появляются быстро и на мобильной сети"

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "8.82"
