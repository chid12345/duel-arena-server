"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.7.00"
VERSION_LABEL = "v2.7.00 — fix: BattleSelectHTML — HP-блок невидим. _close() вызывался ДО проверки HP → тост появлялся под guard-блокером на canvas и игрок не видел причину. Теперь: HP проверяется ДО _close(), overlay остаётся открытым, кнопки серые (bs-blocked), при тапе встряска предупреждения (bsShake) внутри overlay с haptic."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "11.70"
