"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.21.22"
VERSION_LABEL = "v2.21.22 — Balance Этап 5B: set_id для 64 предметов экипировки (детерминированный парсер `<slot>_<rarity><num>`) + 16 оружий (парсер `<type>_<rarity>`) + 53 классов гардероба (ручной маппинг в config/class_set_mapping.py). resolve_active_sets теперь принимает current_class → виртуальный armor-слот, позволяет собирать полный 6-комплект. Распределение ~ 12/12/12/12/9/9 предметов на архетип. 234/234 зелёные."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "16.73"
