"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.22.32"
VERSION_LABEL = "v2.22.32 — фикс: статус-оверлей оплаты не показывался при ВОЗВРАТЕ из CryptoBot. На телефоне после оплаты миниапп перезагружается → первый PaymentStatus.show (из fresh-покупки) теряется, а _resumePolling шёл с fresh:false и статус НЕ показывал. Теперь legendary_armor2/polling.js::_resumePolling показывает «⚡ Проверяю оплату…» заново при возврате (если есть свежий pending la2PendingInvoice типа buy). Кнопка «Я уже оплатил» там же триггерит повторный resume. version.py →2.22.32, GAME_VERSION →17.82."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "17.82"
