"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.6.90"
VERSION_LABEL = "v2.6.90 — премиум-визуал «Мягкая Аура»: убраны радиальные круги-фоны у слотов и табов. Вместо них — Aura самого силуэта (tintFill-копия PNG в цвете редкости/таба, +18%, alpha с pulse 1.5с) + Ambient floor (мягкое цветное пятно под иконкой = «предмет светит на поверхность»). Активный таб — aura ярче ×2.5. preFX по-прежнему НЕ используется (Android safe)."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "11.60"
