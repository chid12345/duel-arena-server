"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.22.47"
VERSION_LABEL = "v2.22.47 — аудит «ТЕСТ ИГРЫ», этап 2 (бой), #2 шаг 2/3 (серверный засчёт пропуска в мини-аппе). Новый jobs/afk_sweep.py: фоновый свипер (фоновый луп _afk_sweep_loop в api_server.py, раз в 3с) проходит активные TMA-бои (opознаются по ui_message is None) и для просроченных (next_turn_deadline + фора 3с) вызывает process_turn_timeout — наказание за пропуск засчитывается даже если игрок закрыл приложение. Это чинит «вечное зависание PvP», когда соперник вышел: бой доводится до конца по AFK. Telegram-бои свипер не трогает (у них свой таймер), босс не участвует (отдельная система). Доставка результата клиенту пока через штатный поллинг /api/battle/state (мгновенный WS-пуш будет в шаге 3/3 вместе с клиентом). Тест tests/test_afk_sweep.py (3 кейса: обрабатывает просроченный TMA-бой, пропускает Telegram-бой, пропускает свежий). Дальше шаг 3/3 (клиент перестаёт ходить наугад + WS-пуш). version.py →2.22.47, GAME_VERSION →17.97."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "17.97"
