"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.22.24"
VERSION_LABEL = "v2.22.24 — серверный авто-разбор оплаченных CryptoPay-счетов (гарантия выдачи без webhook). Новый jobs/payment_settler.py: settle_recent_pending() каждые 30 сек проходит по свежим (≤3ч) невыданным счетам, через идемпотентный recover_one спрашивает CryptoPay статус и выдаёт всё оплаченное — даже если игрок закрыл игру. Запускается фоновым loop _payment_settle_loop в api_server.py::_lifespan (рядом с WB-тиками), settle синхронна → через asyncio.to_thread. Окно 3ч + лимит 40 за проход чтобы не нагружать API. Защита от дублей наследуется от recover_one (флаг items_delivered). Тесты: tests/test_payment_settler.py (фильтр свежих/выданных + вызов recover). Это решает корень «оплатил-не дали» для ВСЕХ покупок, не только брони. version.py →2.22.24, GAME_VERSION →17.75."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "17.75"
