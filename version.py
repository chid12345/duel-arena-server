"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.6.91"
VERSION_LABEL = "v2.6.91 — fix: кнопка «В БОЙ» в профиле — робастный путь. Сбрасываем scene.input.enabled (мог зависнуть false после HTML-оверлея экипировки, закрытого свайпом/back), сбрасываем камеру в (0,0) (после прокрутки панель уезжала за экран), всегда destroy+rebuild battle-панели (свежие HP/gold + лечение сломанного контейнера). _switchTab также сбрасывает scroll — попутно для всех табов."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "11.61"
