"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.22.38"
VERSION_LABEL = "v2.22.38 — восстановление HP вне боя: (1) порог входа в бой HP_MIN_BATTLE_PCT 15%->70% — нельзя в бой пока HP не набралось до 70% (как «энергия»), во всех гейтах (бот/мини-апп/Титан/очередь); (2) «% скорость восстановления» (regen_speed_pct с ботинок) теперь РЕАЛЬНО ускоряет набор HP вне боя — раньше показывалась в «Герой->Бонусы», но не работала. apply_hp_regen* суммируют regen_speed_pct надетых вещей -> mult += speed/100. Тест test_regen_speed_pct_boosts_recovery. Поверх «защита тела легендарке». GAME_VERSION ->17.88."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "17.88"
