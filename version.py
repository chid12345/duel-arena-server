"""
version.py — текущая версия проекта Duel Arena.
Обновляется при каждом значимом изменении.
"""

VERSION = "2.22.46"
VERSION_LABEL = "v2.22.46 — аудит «ТЕСТ ИГРЫ», этап 2 (бой), #2 шаг 1/3 (логика наказания за пропуск). Обобщён раунд-пропуск: _execute_round_afk_human → _execute_round_afk (battle_system/mixins/execute_afk.py) — теперь работает для PvE И PvP, для любого из двоих (или обоих): пропустивший бьёт на 0 и получает ЧИСТЫЙ удар без блока/уклона (is_afk у цели), контрудара нет. process_turn_timeout (timer.py) в PvP теперь зовёт _execute_round_afk вместо _execute_round (раньше в PvP пропуск давал «удар по умолчанию ТУЛОВИЩЕ», а не наказание). 3 пропуска = поражение (как было). Босс не затронут (отдельная система). Тест tests/test_afk_round.py (4 кейса: PvP один молчит, оба молчат, 3 страйка, PvE). Дальше шаг 2/3 (фоновый свипер для мини-аппа) и 3/3 (клиент перестаёт ходить наугад). version.py →2.22.46, GAME_VERSION →17.96."

# Игровая версия для UI (bot / mini app). Один источник истины.
# При деплое с изменениями кода увеличивать на +0.01 (например 2.01 → 2.02).
GAME_VERSION = "17.96"
